
<%= turbo_stream.remove "line-item-#{@line_item.id}" do %>
<% end %>

<%= turbo_stream.replace "cart-counter" do %>
  <div id="cart-counter" class="counter">
    <span><%= @cart_counter %></span>
  </div>
<% end %>

<%= turbo_stream.replace "discount-wrapper" do %>
  <%= render partial: "carts/templates/cart_discount_container", locals: { cart: @current_cart } %>
<% end %>